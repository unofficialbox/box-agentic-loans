# Auto detect text files and perform LF normalization
* text=auto

# Generated binary documents must not be treated as text diffs.
*.pdf binary
*.docx binary

# Sample PDF static resources are binary.
los-salesforce-project/force-app/main/default/staticresources/Los_Sample_*.resource binary
